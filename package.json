{
  "name": "turbogoblinizex",
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "build": "bun build src/index.tsx --outdir dist"
  },
  "dependencies": {
    "hono": "^4.4.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typed-htmx": "^0.3.1"
  }
}
